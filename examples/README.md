# Transfer demo

`MemoryBox-Transfer-Demo.mboxpack` is a non-sensitive example package containing one synthetic CRAB project conversation memory. Use it to test `inspect-pack`, `import-pack`, the UI import flow, or Windows double-click association.
